<div class="fixed-costs-title">
  <h1>User一覧</h1>
</div>

<div class="container">
	<div class="row-fix">
		<div class="col-xs-12">
		  <table class="list-table">

				<tr>
					<th>名前</th>
					<th>性別</th>
					<th>年齢</th>
					<th>家賃</th>
					<th>家族構成</th>
					<th></th>
					<th></th>
			  </tr>

			  <% @users.each do |user| %>

				  <tr>
						<td><%= user.name %></td>
						<td><%= user.sex %></td>
						<td><%= user.old %></td>
						<td><%= user.fixed_cost.rent %>円</td>
						<td><%= user.member %>人</td>
						<td><%= link_to "収入", incomes_path(user.id) %></td>
						<td><%= link_to "支出", costs_path(user.id) %></td>
						<
					</tr>

				<% end %>

　			</table>

    </div>
  </div>
</div>


